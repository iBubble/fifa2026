📅 2026-06-14 22:43 (CST)

✅ Done:
- 更改后端 `five_plays.go` 中玩法未开售的默认输出字符串为 `"不可售"`，替代原先的 `"未开售"`。
- 升级前端 `app.js` 玩法卡片的渲染，当选项包含 `"不可售"` 或是未开售状态时，统一样式展现为灰色字样 `"不可售"`，隐藏具体的推荐方案名称与 `@0.00` 赔率字样，且将几率变更为 `"--"`，保证界面逻辑真实合规。
- 重新跑通 `go build` 与所有单元测试，并完成容器编译重建部署。

📅 2026-06-14 22:48 (CST)

✅ Done:
- 修复 `sporttery.go` 中对世预赛的赔率过滤 Bug。去除了必须包含“世界杯”联赛字样的硬编码过滤条件，使“世预赛”（例如德国 vs 库拉索，伊拉克 vs 挪威）等赛事的真实官方赔率能够被正确拉取和匹配。
- 使得世预赛由于没有开售胜平负而在官方赔率中返回 `0.0` 时，系统不再使用 Elo 仿真参考赔率将其覆盖，而是能够被正确的不可售逻辑捕获并在前端展示为“不可售”（不显示推荐方案）。
- 重新跑通所有单元测试并成功重新构建重启 Docker 部署。

⏳ To-Do:
- 针对极端强队开售超高让球盘进行单场与串关收益比对测试。
