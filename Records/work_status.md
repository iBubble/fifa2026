📅 2026-06-28 23:31 (CST)

✅ Done (核心突破，限3条极简 bullet):
- 彻底完成大单体 `app.js` 的解耦拆分，将其分拆为了 `api.js`（接口）、`render.js`（组件渲染）、`saved.js`（历史复盘）、`chat.js`（AI对话）、`bet_advice.js`（投注方案）这5个职责单一、每个都在 100~200 行之间的微型模块，彻底根治了 IDE 语言服务器处理 AST 的高 CPU 负载。
- 修复了由于 `charts.js` 缺失 `API_BASE` 的定义以及前端多模块加载时 `API_BASE` 变量重定义引发的 JS 语法阻塞错误，将 `API_BASE` 改为单例 `var` 全局变量放置于首个脚本。
- 找回并补全了在精简中控代码时被误删的 `initScheduleStandings`, `initBetAdvice`, `calculatePayoutRange` 等 7 个投注与弹窗辅助函数。

📅 2026-06-28 23:59 (CST)

✅ Done (核心突破，限3条极简 bullet):
- 使用 `git reset --hard origin/main` 强行将工作区重置到 GitHub 最新干净版本，彻底放弃了所有前端 `app.js` 拆分逻辑和相关的后端大模型去耦合改动。
- 清理了工作区中未被跟踪的临时拆分 JS 脚本，将所有前端逻辑完全还原为先前的单文件 `app.js` 模式。
- 执行 `docker-compose up --build -d` 重新构建并拉起 Docker 服务，彻底消除编译故障，恢复正常的本地运行环境。

📅 2026-06-29 00:15 (CST)

✅ Done (核心突破，限3条极简 bullet):
- 在 `worldcup26_sync.go` 中接入了 API 的淘汰赛 ID 精准对齐机制，打通了淘汰赛的实际参赛国家名自动从 API 同步更新至本地数据库 matches 表的整条数据流。
- 重构了前端 `app.js` 的 `loadMatches` 赛程卡片增量更新与冷启动模版，在队名为 `"0"` 时读取全局占位配置，优雅呈现组别占位符（如 `A组第二`、`74场胜者`），彻底解决了赛程选择器列表呈现 `0 vs 0` 的视觉与交互漏洞。
- 修复了“赛程积分表”中“淘汰赛对阵图”与后端实际队名的联动更新，实现了带有权威中英文国旗标签的完整淘汰赛晋级对阵树自动渲染，无任何 `ReferenceError` 运行时控制台报错。

⏳ To-Do (待办事项，限3条极简 bullet):
- 观察在更多淘汰赛场次真实完赛（FT 状态）后，比分动态回填对阵树下一轮节点的刷新敏捷度。
- 校验进入淘汰赛后，滚球监控及多场混合过关精算在单 JS 大文件架构下的整体加载性能表现。
