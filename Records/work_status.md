📅 2026-06-14 22:55 (CST)

✅ Done:
- 彻底移除后端中所有涉及 Elo 仿真赔率兜底的生成逻辑（包括 `main.go`, `five_plays.go`, `lottery.go`, `parlay.go`），防止任何因在官方未开售/停售时仍生成废单、对用户产生误导的逻辑。
- 对竞彩官方确实未售的场次/玩法，系统坚决不生成 any 投注预测和选项，直接列入 `EXCLUDED`（体彩未开盘）拦截，或者在面板上全部真实标识为 `"不可售"`（不显示具体推荐方案）。
- 重新编译、跑通全部测试，并成功 up --build 重构部署容器。

📅 2026-06-14 22:58 (CST)

✅ Done:
- 重构 `sporttery.go` 中 `matchTeam` 的匹配算法，引入极其健壮的中文到英文模糊翻译映射，并强制容错变音符号（如 ç/ã 归一化为 c/a ）、“队”字后缀和多余空格等，解决德国 vs 库拉索、伊拉克 vs 挪威在数据库与官网接口间由于字符或命名细微差别导致队名匹配失败的致命 Bug。
- 确保匹配成功后，官网已开售但未开常规盘的世界杯正赛能正确被 `IsAvailable = true` 加载，仅让未售的胜平负显示为“不可售”，而开售的比分、总进球数、让球等一切玩法正常工作。
- 重新构建重启 Docker 并同步推送远程 GitHub。

⏳ To-Do:
- 针对极端强队开售超高让球盘进行单场与串关收益比对测试。
